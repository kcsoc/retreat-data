import { passwordCheckHandler } from "@storyofams/next-password-protect";

export default passwordCheckHandler("HareKrishna108!", {
  // Options go here (optional)
  cookieName: "next-password-protect",
});
